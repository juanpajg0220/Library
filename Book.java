public class Book extends Material {
    public Book(String identifier, String title, Date registrationDate, int quantity) {
        super(identifier, title, registrationDate, quantity);
    }
}
